# greeting function in python
def area_triangle(base, height):
    return base * height / 2
    area_a = area_triangle(3, 4)
    area_b = area_triangle(2, 5)
    Tota = (area_b + area_b)
    print("The sum of the both area is " + str(area_a))